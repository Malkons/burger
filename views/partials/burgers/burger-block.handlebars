<li>
	{{name}}

	<button class="change-burger" data-id="{{id}}" data-newburger="{{eaten}}">
		{{#if eaten}}Lets Eat!{{else}}Nothing!!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>